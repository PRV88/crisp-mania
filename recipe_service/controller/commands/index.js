const { addRecipeController } = require("./addRecipe");
const { editRecipeController } = require("./editRecipe");
const { removeRecipeController } = require("./removeRecipe");
module.exports = {
    addRecipeController,
    editRecipeController,
    removeRecipeController
}